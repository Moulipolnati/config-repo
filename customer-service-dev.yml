global:
  message: welcome to customer service dev environment!!

server:
  port: 8081   # keep consistent

message: hello from dev!

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices
    username: root
    password: Mouli@3022  # ← put your real password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
